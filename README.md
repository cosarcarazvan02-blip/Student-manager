# Student Manager (C)

Console application for managing students in C.
Uses structs, dynamic memory (malloc/realloc), and basic CRUD operations.

Build:
gcc main.c -o student_manager
./student_manager
